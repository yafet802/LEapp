import 'package:flutter/material.dart';

class BotonActividad extends StatelessWidget {
  const BotonActividad({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}